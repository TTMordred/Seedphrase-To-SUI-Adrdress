{
  "name": "sui-address-generator",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp index.html dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@mysten/sui.js": "^0.49.1",
    "@scure/bip39": "^1.2.2",
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  }
}